<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <artifactId>pdf-converter-parent</artifactId>
        <groupId>no.kantega</groupId>
        <version>0.2-SNAPSHOT</version>
    </parent>

    <artifactId>pdf-converter-transformer-msoffice</artifactId>
    <packaging>pom</packaging>

    <modules>
        <module>pdf-converter-transformer-msoffice-word</module>
        <module>pdf-converter-transformer-msoffice-excel</module>
        <module>pdf-converter-transformer-msoffice-base</module>
        <module>pdf-converter-transformer-msoffice-test</module>
    </modules>

    <properties>
        <test.office.skip>true</test.office.skip>
    </properties>

    <dependencies>
        <!-- Local dependencies -->
        <dependency>
            <groupId>no.kantega</groupId>
            <artifactId>pdf-converter-transformer-api</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>no.kantega</groupId>
            <artifactId>pdf-converter-util-transformer-process</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>ms-office</id>
            <properties>
                <test.office.skip>false</test.office.skip>
            </properties>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${version.maven.surefire-plugin}</version>
                <configuration>
                    <skipTests>${test.office.skip}</skipTests>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
